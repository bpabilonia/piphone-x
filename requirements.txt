# PiPhone-X Requirements
# For Raspberry Pi 3+ 32-bit OS

# Serial communication with SIM7600X HAT
pyserial>=3.5

# GPIO access for Raspberry Pi
# Note: Install via apt on Raspberry Pi: sudo apt install python3-rpi.gpio
# RPi.GPIO>=0.7.1

# For async operations
schedule>=1.2.0

# Note: The following packages are better installed via apt on Raspberry Pi:
#   sudo apt install python3-pil python3-pil.imagetk
#   sudo apt install python3-pyaudio
# 
# Only uncomment if installing on non-Raspberry Pi systems:
# Pillow>=9.0.0
# pyaudio>=0.2.13

